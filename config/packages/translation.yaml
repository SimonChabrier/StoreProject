framework:
    default_locale: fr # The default locale
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - en # The fallback locale
            - fr # The fallback locale
#        providers:
#            crowdin:
#                dsn: '%env(CROWDIN_DSN)%'
#            loco:
#                dsn: '%env(LOCO_DSN)%'
#            lokalise:
#                dsn: '%env(LOKALISE_DSN)%'
