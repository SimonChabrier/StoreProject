<form method="post" action="{{ path('app_configuration_delete', {'id': configuration.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette configuration ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ configuration.id) }}">
    <button class="btn">Supprimer</button>
</form>
