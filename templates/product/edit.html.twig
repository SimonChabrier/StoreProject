{% extends 'base.html.twig' %}

{% block title %}Modifierer {{ product.name }}{% endblock %}

{% block content %}
    <h1>Modifierer {{ product.name }}</h1>

 <div class="buttonContainer"> 
    {{ include('product/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('product/_delete_form.html.twig') }}
    <a href="{{ path('app_product_index') }}" class="btn">Retour à la liste</a>
</div>
{% endblock %}
