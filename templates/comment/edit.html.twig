{% extends 'base.html.twig' %}

{% block title %}Modifier le commentaire {{ comment.id }}{% endblock %}

{% block content %}
    <h1>Modifier le commentaire {{ comment.id }}</h1>

<div class="buttonContainer">
    {{ include('comment/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <a href="{{ path('app_comment_index') }}" class="btn">Retour à la liste</a>
    {{ include('comment/_delete_form.html.twig') }}
</div>
{% endblock %}
