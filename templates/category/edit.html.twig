{% extends 'base.html.twig' %}

{% block title %}Modifierer la catégorie {{ category.name }}{% endblock %}

{% block content %}
    <h1>Modifierer la catégorie {{ category.name }}</h1>

    {{ include('category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_category_index') }}" class="btn">Retour à la liste</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
