{% extends 'base.html.twig' %}

{% block title %}ProductAttribute{% endblock %}

{% block body %}
    <h1>ProductAttribute</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_attribute.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_attribute.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ product_attribute.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_attribute_index') }}">back to list</a>

    <a href="{{ path('app_product_attribute_edit', {'id': product_attribute.id}) }}">edit</a>

    {{ include('product_attribute/_delete_form.html.twig') }}
{% endblock %}
