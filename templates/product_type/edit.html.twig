{% extends 'base.html.twig' %}

{% block title %}Modifier ProductType{% endblock %}

{% block content %}
    <h1>Modifier {{ product_type.name}}</h1>
   <div class="buttonContainer"> 
        {{ include('product_type/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <a href="{{ path('app_product_type_index') }}" class="btn">Retour à la liste</a>
    </div>
    {{ include('product_type/_delete_form.html.twig') }}
{% endblock %}
